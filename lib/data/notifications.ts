import type { Notification } from "@/lib/types";

// Legacy mock data — kept for reference but no longer used by the app.
// Real notifications are now stored in the Supabase `notifications` table.
export const NOTIFICATIONS: Notification[] = [];
